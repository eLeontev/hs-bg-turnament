type Player {
    playerId: String!
    playerLogin: String!
}

type PendingGame {
    gameId: ID
    gameName: String!
    authorId: String!
    authorLogin: String!
    createdDate: String!
    players: [Player!]!
}

type PlayGame {
    gameId: String!
    players: [Player]!
    phase: String
}

type Query {
    pendingGames: [PendingGame]!
    playGame(gameId: String!, playerId: String!): PlayGame
}

type Message {
    message: String!
}

type Mutation {
    createPendingGameRequest(
        playerId: String
        playerLogin: String
        gameName: String
    ): Message
    deletePendingGameRequest(gameId: String!, playerId: String): Message
    joinPendingGameRequest(
        gameId: String!
        playerId: String
        playerLogin: String
    ): Message
    leavePendingGameRequest(gameId: String!, playerId: String): Message
    startPendingGameRequest(gameId: String!, playerId: String): Message
}
