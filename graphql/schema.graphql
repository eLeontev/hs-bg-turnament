type Player {
    playerId: String!
    playerLogin: String!
}

type PendingGame {
    gameId: ID
    gameName: String!
    authorId: String!
    authorLogin: String!
    createdDate: String!
    players: [Player!]!
}

type Query {
    pendingGames: [PendingGame]!
    initPlayGameRequest(
        gameId: String!
        playerId: String
        privatePlayerId: String
    ): Message
}

type Message {
    message: String!
}

type Mutation {
    createPendingGameRequest(
        playerId: String
        playerLogin: String
        gameName: String
    ): Message
    deletePendingGameRequest(gameId: String!, playerId: String): Message
    joinPendingGameRequest(
        gameId: String!
        playerId: String
        playerLogin: String
    ): Message
    leavePendingGameRequest(gameId: String!, playerId: String): Message
    startPendingGameRequest(gameId: String!, playerId: String): Message
}
