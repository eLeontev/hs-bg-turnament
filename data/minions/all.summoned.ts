import { minionIds } from '@prisma/client';

import { Minion } from '../../features/play-game/models/play-game.minion.models';

export const allSummoned1StartTavern = new Map<minionIds, Minion>([]);

export const allSummoned2StartTavern = new Map<minionIds, Minion>([]);

export const allSummoned3StartTavern = new Map<minionIds, Minion>([]);

export const allSummoned4StartTavern = new Map<minionIds, Minion>([]);

export const allSummoned5StartTavern = new Map<minionIds, Minion>([]);

export const allSummoned6StartTavern = new Map<minionIds, Minion>([]);
